/**
 * Shared React hooks will live here.
 * Feature-specific hooks belong under features/<name>/hooks.
 */

export function noop(): void {
  // Placeholder export so the hooks package resolves during foundation boot.
}
