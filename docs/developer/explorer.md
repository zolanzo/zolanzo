# API Explorer

Test Public API v1 endpoints interactively in your browser or CLI.

## Capabilities

- Inspect operation schemas and required scope lists
- Generate ready-to-run `curl` commands
- Generate TypeScript client code snippets
- Execute dry-run previews against `/api/v1` routes

Access the API Explorer at `/developer/explorer` or call `/api/v1/developer/explorer/preview`.
