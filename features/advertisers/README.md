# features/advertisers

> **DEPRECATED alias.** Use `features/clients`.

Kept so early architecture references and internal mappings remain stable.
User-facing product language is **Client** (posts work) / **Worker** (completes work).

When implementing, re-export from `features/clients` — do not duplicate logic.
